# Тест-кейсы - Приложение `books`

---

## Модель `Genre`

### TC-GEN-001: Создание жанра с валидными данными

**Предусловие:** БД пуста  
**Шаги:**

1. Создать объект `Genre(name="Фантастика")`
2. Вызвать `full_clean()` → сохранить в БД
3. Получить запись из БД

**Ожидаемый результат:** Запись создана. В БД `name = "фантастика"` (`.lower()` в `save()`).

---

### TC-GEN-002: Создание жанра с пустым `name`

**Предусловие:** БД пуста  
**Шаги:**

1. Создать объект `Genre(name="")`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-GEN-003: Уникальность поля `name`

**Предусловие:** В БД существует запись `Genre(name="фантастика")`  
**Шаги:**

1. Создать второй объект `Genre(name="Фантастика")`
2. Сохранить в БД

**Ожидаемый результат:** Выброшено исключение `IntegrityError`. Дубликат не создан.

> ⚠️ **Примечание:** `save()` приводит к `.lower()` перед записью, поэтому `"Фантастика"` и `"фантастика"` - это один и
> тот же ключ. Кейс проверяет именно это поведение.

---

### TC-GEN-004: Максимальная длина `name` - граничное значение (150 символов)

**Предусловие:** БД пуста  
**Шаги:**

1. Создать объект с `name` длиной ровно 150 символов
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Запись успешно создана.

---

### TC-GEN-005: Превышение максимальной длины `name` (151 символ)

**Предусловие:** БД пуста  
**Шаги:**

1. Создать объект с `name` длиной 151 символ
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-GEN-006: Сохранение приводит `name` к нижнему регистру

**Предусловие:** БД пуста  
**Шаги:**

1. Создать объект `Genre(name="ФАНТАСТИКА")`
2. Сохранить в БД
3. Получить запись из БД

**Ожидаемый результат:** В БД `name = "фантастика"`.

---

### TC-GEN-007: Метод `__str__` возвращает корректное значение

**Предусловие:** Создана запись `Genre(name="Фантастика")`  
**Шаги:**

1. Вызвать `str(genre_instance)`

**Ожидаемый результат:** Возвращается строка `"фантастика"`.

---

## Модель `Book`

### TC-BOK-001: Создание книги с валидными данными

**Предусловие:** В БД существуют `Author`, `Genre`, `User`  
**Шаги:**

1. Создать объект `Book` со всеми обязательными полями: `title`, `pages`, `publication_date`, `isbn`, `age_rating`
2. Добавить `author` и `genre` через M2M
3. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Книга создана. Все поля соответствуют переданным значениям.

---

### TC-BOK-002: Создание книги с пустым `title`

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект
   `Book(title="", pages=300, publication_date="2020-01-01", isbn="9780306406157", age_rating="ABOVE_ZERO")`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-BOK-003: Максимальная длина `title` - граничное значение (150 символов)

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `title` длиной ровно 150 символов, остальные поля валидны
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Запись успешно создана.

---

### TC-BOK-004: Превышение максимальной длины `title` (151 символ)

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `title` длиной 151 символ
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-BOK-005: Создание книги с `pages = 0` - невалидное значение

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `pages=0`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError` - `MinValueValidator(1)` запрещает ноль.

---

### TC-BOK-006: Создание книги с `pages = 1` - граничное валидное значение

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `pages=1`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Запись успешно создана.

---

### TC-BOK-007: Создание книги с отрицательным значением `pages`

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `pages=-1`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-BOK-008: Уникальность поля `isbn`

**Предусловие:** В БД существует книга с `isbn="9780306406157"`  
**Шаги:**

1. Создать вторую книгу с тем же `isbn="9780306406157"`
2. Сохранить в БД

**Ожидаемый результат:** Выброшено исключение `IntegrityError`. Дубликат не создан.

---

### TC-BOK-009: Создание книги с невалидным `isbn`

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `isbn="1234567890ABC"` (не соответствует стандарту ISBN)
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`.

---

### TC-BOK-010: Создание книги со всеми валидными значениями `age_rating`

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Поочерёдно создать книги с каждым из значений: `ABOVE_ZERO`, `ABOVE_SIX`, `ABOVE_TWELVE`, `ABOVE_SIXTEEN`,
   `ABOVE_EIGHTEEN`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Все 5 записей успешно созданы.

---

### TC-BOK-011: Создание книги с невалидным `age_rating`

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `age_rating="ABOVE_HUNDRED"`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-BOK-012: Создание книги без `age_rating` - обязательное поле

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` без поля `age_rating`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-BOK-013: Создание книги без `publication_date` - обязательное поле

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` без поля `publication_date`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-BOK-014: Создание книги без `user` - поле опциональное

**Предусловие:** БД содержит необходимые связанные объекты  
**Шаги:**

1. Создать объект `Book` с `user=None`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Запись успешно создана. Поле `user` равно `NULL`.

---

### TC-BOK-015: Удаление `user` - поле `user` в книге становится `NULL`

**Предусловие:** В БД существует `Book` с привязанным `User`  
**Шаги:**

1. Удалить объект `User`
2. Получить книгу из БД

**Ожидаемый результат:** Книга не удалена. Поле `user` стало `NULL`.

---

### TC-BOK-016: Привязка нескольких авторов к книге (M2M)

**Предусловие:** В БД существуют 2 объекта `Author` и 1 объект `Book`  
**Шаги:**

1. Добавить обоих авторов к книге через `book.author.set([author1, author2])`
2. Получить список авторов книги

**Ожидаемый результат:** Книга связана с двумя авторами.

---

### TC-BOK-017: Привязка нескольких жанров к книге (M2M)

**Предусловие:** В БД существуют 2 объекта `Genre` и 1 объект `Book`  
**Шаги:**

1. Добавить оба жанра к книге через `book.genre.set([genre1, genre2])`
2. Получить список жанров книги

**Ожидаемый результат:** Книга связана с двумя жанрами.

---

### TC-BOK-018: Свойство `is_for_child` - возвращает `True` для `ABOVE_ZERO` и `ABOVE_SIX`

**Предусловие:** Созданы книги с `age_rating="ABOVE_ZERO"` и `age_rating="ABOVE_SIX"`  
**Шаги:**

1. Обратиться к `book.is_for_child` для каждой книги

**Ожидаемый результат:** Оба возвращают `True`.

---

### TC-BOK-019: Свойство `is_for_child` - возвращает `False` для `ABOVE_TWELVE`, `ABOVE_SIXTEEN`, `ABOVE_EIGHTEEN`

**Предусловие:** Созданы книги с соответствующими рейтингами  
**Шаги:**

1. Обратиться к `book.is_for_child` для каждой книги

**Ожидаемый результат:** Все три возвращают `False`.

---

### TC-BOK-020: Свойство `is_for_teenager` - возвращает `True` для `ABOVE_TWELVE` и `ABOVE_SIXTEEN`

**Предусловие:** Созданы книги с соответствующими рейтингами  
**Шаги:**

1. Обратиться к `book.is_for_teenager` для каждой книги

**Ожидаемый результат:** Оба возвращают `True`.

---

### TC-BOK-021: Свойство `is_for_adult` - возвращает `True` только для `ABOVE_EIGHTEEN`

**Предусловие:** Созданы книги с разными рейтингами  
**Шаги:**

1. Обратиться к `book.is_for_adult` для каждой книги

**Ожидаемый результат:** `True` только у книги с `age_rating="ABOVE_EIGHTEEN"`, остальные `False`.

---

### TC-BOK-022: Метод `__str__` возвращает корректное значение

**Предусловие:** Создана книга с `title="Мастер и Маргарита"`  
**Шаги:**

1. Вызвать `str(book_instance)`

**Ожидаемый результат:** Возвращается строка `"Мастер и Маргарита"`.

---

## Модель `BookInventory`

### TC-INV-001: Создание записи инвентаря с валидными данными

**Предусловие:** В БД существует объект `Book`  
**Шаги:**

1. Создать объект `BookInventory(book=<объект>, status="AVAILABLE")`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Запись создана. Поля соответствуют переданным значениям.

---

### TC-INV-002: Создание нескольких экземпляров одной книги

**Предусловие:** В БД существует объект `Book`  
**Шаги:**

1. Создать 3 объекта `BookInventory` с одной и той же книгой и статусом `AVAILABLE`
2. Сохранить все 3 в БД

**Ожидаемый результат:** Все 3 записи созданы - модель допускает несколько экземпляров одной книги.

---

### TC-INV-003: Создание записи со всеми валидными значениями `status`

**Предусловие:** В БД существует объект `Book`  
**Шаги:**

1. Поочерёдно создать записи со статусами: `AVAILABLE`, `BUSY`, `RESERVED`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Все 3 записи успешно созданы.

---

### TC-INV-004: Создание записи с невалидным `status`

**Предусловие:** В БД существует объект `Book`  
**Шаги:**

1. Создать объект `BookInventory(book=<объект>, status="LOST")`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-INV-005: Создание записи без `status` - обязательное поле

**Предусловие:** В БД существует объект `Book`  
**Шаги:**

1. Создать объект `BookInventory(book=<объект>)` без `status`
2. Вызвать `full_clean()` → сохранить в БД

**Ожидаемый результат:** Выброшено исключение `ValidationError`. Запись в БД не создана.

---

### TC-INV-006: Удаление книги - `book` все экземпляры удаляются.

**Предусловие:** В БД существует `BookInventory` с привязанной `Book`  
**Шаги:**

1. Удалить объект `Book`
2. Получить запись `BookInventory` из БД

**Ожидаемый результат:** Все экземпляры книги `BookInventory` удалены.

> ⚠️ **Баг:** Сейчас такое поведение отсутствует в бизнес логике проекта.

---

### TC-INV-007: Метод `__str__` при существующей книге

**Предусловие:** Создана запись `BookInventory` с книгой `title="Мастер и Маргарита"`, `isbn="9780306406157"`,
`status="AVAILABLE"`  
**Шаги:**

1. Вызвать `str(inventory_instance)`

**Ожидаемый результат:** Возвращается строка формата `"Мастер и Марга | 9780306406157 -> AVAILABLE"` (первые 15 символов
заголовка).

---

## `BookManager`

### TC-BMG-001: `get_all_books_list()` возвращает все книги

**Предусловие:** В БД созданы 3 книги  
**Шаги:**

1. Вызвать `Book.objects.get_all_books_list()`

**Ожидаемый результат:** Возвращён `QuerySet` из 3 записей.

---

### TC-BMG-002: `available()` возвращает только книги со статусом `AVAILABLE`

**Предусловие:** В БД 3 книги, у каждой по одному экземпляру в `BookInventory`: `AVAILABLE`, `BUSY`, `RESERVED`  
**Шаги:**

1. Вызвать `Book.objects.available()`

**Ожидаемый результат:** Возвращена 1 книга - та, у которой есть экземпляр со статусом `AVAILABLE`.

---

### TC-BMG-003: `busy()` возвращает только книги со статусом `BUSY`

**Предусловие:** Аналогично TC-BMG-002  
**Шаги:**

1. Вызвать `Book.objects.busy()`

**Ожидаемый результат:** Возвращена 1 книга со статусом `BUSY`.

---

### TC-BMG-004: `reserved()` возвращает только книги со статусом `RESERVED`

**Предусловие:** Аналогично TC-BMG-002  
**Шаги:**

1. Вызвать `Book.objects.reserved()`

**Ожидаемый результат:** Возвращена 1 книга со статусом `RESERVED`.

---

### TC-BMG-005: `search_books()` - поиск по `title`

**Предусловие:** В БД 2 книги: `"Война и мир"`, `"Анна Каренина"`  
**Шаги:**

1. Вызвать `Book.objects.search_books(title="война")`

**Ожидаемый результат:** Возвращена 1 книга - `"Война и мир"`.

---

### TC-BMG-006: `search_books()` - фильтр по `publication_date`

**Предусловие:** В БД книги с разными датами издания  
**Шаги:**

1. Вызвать `Book.objects.search_books(publication_year=2020)`

**Ожидаемый результат:** Возвращены книги для указанного года.

> ⚠️ **Баг:** В `valid_filters` ключ `"publication_year"` маппится на `"publication_year"`, но в модели поле называется
`publication_date`. Фильтр никогда не применяется. После исправления должен маппиться на `"publication_date__year"`.

---

### TC-BMG-007: `search_books()` - передача несуществующего фильтра игнорируется

**Предусловие:** В БД существуют книги  
**Шаги:**

1. Вызвать `Book.objects.search_books(unknown_field="значение")`

**Ожидаемый результат:** Возвращены все книги без фильтрации. Исключений нет.

---

### TC-BMG-008: `recent()` - возвращает книги за последние 5 лет

**Предусловие:** В БД 3 книги: одна текущего года, одна 3 года назад, одна 10 лет назад  
**Шаги:**

1. Вызвать `Book.objects.recent()`

**Ожидаемый результат:** Возвращены 2 книги (текущего года и 3 года назад).

---

### TC-BMG-009: `recent(years=1)` - кастомный период

**Предусловие:** Аналогично TC-BMG-008  
**Шаги:**

1. Вызвать `Book.objects.recent(years=1)`

**Ожидаемый результат:** Возвращена 1 книга - только текущего года.
